apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-poliza-config
  namespace: siniestros
data:
  MYSQL_DATABASE: "polizadb"
---
apiVersion: v1
kind: Secret
metadata:
  name: mysql-poliza-secret
  namespace: siniestros
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "rootpassword"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-poliza-pvc
  namespace: siniestros
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-poliza
  namespace: siniestros
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-poliza
  template:
    metadata:
      labels:
        app: mysql-poliza
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-poliza-secret
              key: MYSQL_ROOT_PASSWORD
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: mysql-poliza-config
              key: MYSQL_DATABASE
        ports:
        - containerPort: 3306
          name: mysql
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql
        livenessProbe:
          exec:
            command:
            - mysqladmin
            - ping
            - -h
            - localhost
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - mysqladmin
            - ping
            - -h
            - localhost
          initialDelaySeconds: 10
          periodSeconds: 5
      volumes:
      - name: mysql-storage
        persistentVolumeClaim:
          claimName: mysql-poliza-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-poliza
  namespace: siniestros
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: mysql-poliza
  clusterIP: None
